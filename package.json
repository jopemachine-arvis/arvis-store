{
  "name": "arvis-store",
  "version": "0.0.1",
  "description": "Add your module to arvis-document, arvis-store and upload it to npm at once",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "rebuild": "rm -rf node_modules && npm i && npm run build",
    "lint": "tslint --project tsconfig.json --fix"
  },
  "keywords": [
    "arvis",
    "arvis-extension"
  ],
  "author": "jopemachine",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^15.12.5",
    "@types/semver": "^7.3.6",
    "husky": "^4.2.5",
    "tslint": "^6.1.3",
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "arvis-extension-validator": "^0.2.0",
    "chalk": "^4.1.1",
    "execa": "^5.1.1",
    "latest-version": "^5.1.0",
    "meow": "^9.0.0",
    "ora": "^5.4.1",
    "semver": "^7.3.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint",
      "pre-push": "npm run lint"
    }
  }
}
